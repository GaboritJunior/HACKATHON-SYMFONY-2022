{% extends 'base.html.twig' %}

{% block title %}CharacterResource{% endblock %}

{% block body %}
    <h1>CharacterResource</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ character_resource.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ character_resource.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_character_resource_index') }}">back to list</a>

    <a href="{{ path('app_character_resource_edit', {'id': character_resource.id}) }}">edit</a>

    {{ include('character_resource/_delete_form.html.twig') }}
{% endblock %}
